<div class="container">
    <section>
        <div class="row">
        </div>
        <div class="row">
            <div>
                <div class ="panel panel-primary">
                    <div class="panel-body">
                        <div class="row">
                            <div class="col-md-3">

                            </div>
                            <div class="col-md-6 text-center">
                                <!-- grey -->
                                @Html.ActionLink("Meu Perfil", "Detalhes", new { id = Model.Id }, new { @class = "btn btn-primary btn-large" })
                                @Html.ActionLink("Editar Perfil", "Editar", new { id = Model.Id }, new { @class = "btn btn-primary btn-large" })
                                @Html.ActionLink("Adicionar Campo", "AdicionarCampo", new { id = Model.Id }, new { @class = "btn btn-primary btn-large" })
                            </div>
                        </div>
                        <div class="row">
                            <div class="col-md-6">
                            </div>
                        </div>
                        <div class="row">
                            <div class="col-md-3">

                            </div>
                            <div class="col-md-6 text-center">
                                <!-- dblue -->
                                @Html.ActionLink("Gerenciar Campos", "EditarCampo", new { id = Model.Id }, new { @class = "btn btn-primary btn-large" })
                                @Html.ActionLink("Visualizar Aluguel(is)", "ListarAlugueis", new { id = Model.Id }, new { @class = "btn btn-primary btn-large" })
                                @Html.ActionLink("Alugueis Pagos", "VerPagamentos", new { id = Model.Id }, new { @class = "btn btn-primary btn-large" })
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
</div>




